  <button class= "btn btn-secondary btn-lg" ><%= link_to "list new", new_list_path %>
  </button>
<div class="cards">
  <% @lists.each do |list|%>
  <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%=list.image_url%>)">
    <%= list.name%>
  </div>
  <% end%>
   </div>
